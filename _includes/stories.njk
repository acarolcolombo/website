{% extends "page.njk" %}

{% block content %}

    <h1>{{ title }}</h1>

    <span class="story-date">{{ date.toISOString().split('T')[0].replace('-', '/').replace('-', '/') }}</span>

    {{ content | safe }}

{% endblock %}
